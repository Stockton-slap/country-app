const CountriesItem = ({ country }) => {
  const {
    borders,
    capital,
    continents,
    currencies,
    flags: { png },
    languages,
    population,
    area,
  } = country;

  return <div>ITEM</div>;
};

export default CountriesItem;
